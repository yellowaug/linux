=====================================================================
sql server 
=====================================================================
说明：
not null 不允许为空
identity(1,1) 从1开始自增长，每次+1
primary key 设为主键
=====================================================================
建表：
create table 表名
(
 id int identity(1,1) primary key not null,
 字段1 int not null,
 字段2 int 
)